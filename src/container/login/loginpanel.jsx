"use strict";

var React = require('react');

var LoginPanel = React.createClass({
    displayName: "LoginPanel",
    render: function(){
        return (
            <div className="login-panel">Login</div>
        )
    }
});

module.exports = LoginPanel;

